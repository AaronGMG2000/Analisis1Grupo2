class ShoppingHistory {
  constructor({ id, fecha, tipo, monto, dias_alquiler,pelicula}) {
    this.id = id
    this.fecha = fecha
    this.tipo = tipo
    this.monto = monto
    this.dias_alquiler = dias_alquiler
    this.pelicula = pelicula
  }
}

export default ShoppingHistory
